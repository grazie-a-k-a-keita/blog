---
import { cn } from "@/lib/utils";

const { title, subTitle } = Astro.props;
---

<div>
	<h2 class={cn("mb-8 text-center font-bold text-2xl", "mb-16")}>
		{title}
	</h2>
	<p class="text-muted-foreground mb-16 text-center">{subTitle}</p>
</div>
